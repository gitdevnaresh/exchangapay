export interface tranUpdate {
    date: string,
    id: string,
    transactionStatus: string,
};

export interface tranDetails {
    amount: number,
    feeComissions: number,
    feeConverted: number,
    id: string,
}