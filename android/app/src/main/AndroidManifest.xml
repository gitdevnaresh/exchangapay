<manifest xmlns:android="http://schemas.android.com/apk/res/android"
 xmlns:tools="http://schemas.android.com/tools">

  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.CAMERA" />
  <!-- The configured KYC and app flows do not record audio. Prevent Sumsub's
       transitive Android dependency from adding microphone access to the APK. -->
  <uses-permission
    android:name="android.permission.RECORD_AUDIO"
    tools:node="remove" />
  <!-- Legacy storage permissions are only applicable before Android 13. -->
  <uses-permission
    android:name="android.permission.READ_EXTERNAL_STORAGE"
    android:maxSdkVersion="32"
    tools:replace="android:maxSdkVersion" />
  <uses-permission
    android:name="android.permission.WRITE_EXTERNAL_STORAGE"
    android:maxSdkVersion="28"
    tools:replace="android:maxSdkVersion" />
  <!-- Android 13+ replacement for reading an image selected outside the system picker. -->
  <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
  <uses-permission android:name="android.permission.VIBRATE"/>
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <!-- Local notifications do not need a persistent CPU wake lock. Remove any
       declaration contributed by the legacy notification dependency as well. -->
  <uses-permission
    android:name="android.permission.WAKE_LOCK"
    tools:node="remove" />
  <!-- These are contributed by SDKs but are not used by the app. Location is
       not part of the configured KYC flow; notifications do not use alarms;
       and no overlay window is required. -->
  <uses-permission
    android:name="android.permission.ACCESS_COARSE_LOCATION"
    tools:node="remove" />
  <uses-permission
    android:name="android.permission.ACCESS_FINE_LOCATION"
    tools:node="remove" />
  <uses-permission
    android:name="android.permission.SCHEDULE_EXACT_ALARM"
    tools:node="remove" />
  <uses-permission
    android:name="android.permission.SYSTEM_ALERT_WINDOW"
    tools:node="remove" />
  <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove" />

  <application android:name=".MainApplication" android:label="@string/app_name" android:icon="@mipmap/ic_launcher" android:roundIcon="@mipmap/ic_launcher_round" android:allowBackup="false" android:dataExtractionRules="@xml/data_extraction_rules" android:networkSecurityConfig="@xml/network_security_config" android:theme="@style/AppTheme" android:supportsRtl="true">
    <activity android:name=".MainActivity" android:label="@string/app_name" android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode" android:launchMode="singleTask" android:windowSoftInputMode="adjustPan" android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
      <!-- M-07: App Links intent filter. android:autoVerify="true" causes Android
           to verify ownership of the domain against the hosted assetlinks.json
           before registering this filter. A verified App Link cannot be claimed
           by another app — unlike the custom scheme below, which any app can
           register. Requires /.well-known/assetlinks.json to be hosted at
           https://exchangapay.com (see security/M07_APPLINKS_SETUP.md).
           The custom-scheme filter below is kept during migration so existing
           installs continue to work until the App Link is verified. -->
      <intent-filter android:autoVerify="true">
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="https" android:host="${auth0Domain}" android:pathPrefix="/android/${applicationId}/callback" />
      </intent-filter>
      <!-- M-07: Legacy custom-scheme callback. Keep until App Links are verified
           on all active installs, then remove this filter and the auth0Scheme
           placeholder in build.gradle. -->
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="${auth0Scheme}" android:host="callback" />
      </intent-filter>
    </activity>
  </application>
</manifest>