# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock


# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage
*.lcov

# nyc test coverage
.nyc_output

# Grunt intermediate storage (https://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/
jspm_packages/

# Snowpack dependency directory (https://snowpack.dev/)
web_modules/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variable files
.env
.env.*
!.env.example

# Sentry secrets
# NOTE: ios/sentry.properties AND android/sentry.properties are intentionally
# TRACKED but hold ONLY the non-secret org/project/url defaults, so a fresh clone
# builds with no local setup. (android/sentry.properties had gone missing, which
# broke assembleRelease outright — sentry-cli aborted with "organization ID or
# slug is required" before R8 ever ran.) The auth token must never be committed —
# sentry-cli reads it from SENTRY_AUTH_TOKEN (env var in CI, shell export or
# ~/.sentryclirc locally). Everything below can contain a token, so it stays out.
.sentryclirc
sentry.properties.local
ios/sentry.properties.local
android/sentry.properties.local
.env.sentry-build-plugin

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next
out

# Nuxt.js build / generate output
.nuxt
dist

# Gatsby files
.cache/
# Comment in the public line in if your project uses Gatsby and not Next.js
# https://nextjs.org/blog/next-9-1#public-directory-support
# public

# vuepress build output
.vuepress/dist

# vuepress v2.x temp and cache directory
.temp
.cache

# Sveltekit cache directory
.svelte-kit/

# vitepress build output
**/.vitepress/dist

# vitepress cache directory
**/.vitepress/cache

# Docusaurus cache and generated files
.docusaurus

# Serverless directories
.serverless/

# FuseBox cache
.fusebox/

# DynamoDB Local files
.dynamodb/

# Firebase cache directory
.firebase/

# TernJS port file
.tern-port

# Stores VSCode versions used for testing VSCode extensions
.vscode-test

# yarn v3
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Vite logs files
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# React Native
# Metro bundler cache
.metro-health-check*

# Package lock files
# NOTE: package-lock.json is intentionally TRACKED. This is an application, not a
# library: an uncommitted lockfile lets every install resolve differently, which is
# how react-native-reanimated 4.5.3 (which requires RN 0.83+) ended up installed
# against RN 0.81. Keep it committed so builds are reproducible.
yarn.lock

# Bin directory (CocoaPods binaries)
bin/

# Android - Comprehensive gradle and build files
android/app/build/
android/build/
android/.gradle/
android/gradle/
android/gradlew
android/gradlew.bat
android/local.properties
android/app/debug.keystore
android/app/release.keystore
android/app/*.keystore
android/app/*.jks
android/app/.cxx/
android/app/src/main/assets/index.android.bundle
android/app/src/main/assets/index.android.map
android/app/src/main/res/drawable-*
android/app/src/main/res/raw/
android/app/src/main/res/values/strings.xml.bak
android/app/src/main/res/values/colors.xml.bak
android/app/src/main/res/values/styles.xml.bak
android/app/src/main/res/values/themes.xml.bak
android/app/src/main/res/values-night/strings.xml.bak
android/app/src/main/res/values-night/colors.xml.bak
android/app/src/main/res/values-night/styles.xml.bak
android/app/src/main/res/values-night/themes.xml.bak

# Android gradle wrapper and properties
android/gradle/wrapper/gradle-wrapper.jar
android/gradle/wrapper/gradle-wrapper.properties
android/gradle.properties
android/settings.gradle
android/build.gradle
android/app/build.gradle

# iOS CocoaPods
ios/Pods/
ios/build/
ios/.cxx/
ios/*.xcworkspace/xcuserdata/
ios/*.xcodeproj/xcuserdata/
ios/*.xcodeproj/project.xcworkspace/xcuserdata/
ios/Podfile.lock

# iOS build generated files
ios/build/generated/
# NOTE: PrivacyInfo.xcprivacy is intentionally TRACKED. It is a required App Store
# submission artifact and is referenced by project.pbxproj's Copy Bundle Resources.

# Ruby/CocoaPods
vendor/
gems/
cache/
specifications/
extensions/
Gemfile.lock

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Thumbnails
._*

# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Directories potentially created on remote AFP share
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Temporary files
*.tmp
*.temp

# OS generated files
Thumbs.db
ehthumbs.db
Desktop.ini

